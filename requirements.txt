requests>=2.31.0
python-dateutil>=2.9.0
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
pydantic-settings>=2.2
httpx>=0.27
SQLAlchemy>=2.0
# Optional Postgres backend for the paper-trade journal. Required when
# POLY_JOURNAL_URL is set to a postgres:// connection string (e.g. Supabase,
# Vercel Postgres, Neon). The [binary] extra ships prebuilt wheels so no
# libpq install step is needed on Vercel/Linux/macOS.
psycopg[binary]>=3.1.18
numpy>=1.26
pandas>=2.0
scipy>=1.11
streamlit>=1.36
plotly>=5.20
python-dotenv>=1.0
tenacity>=8.2
structlog>=24.0
websockets>=12
ruff>=0.5
pytest-asyncio>=0.23
